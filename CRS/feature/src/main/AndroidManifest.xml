<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.mcgill.hci.crs_application.feature">

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application>
        <service
            android:name=".NotificationListener"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity android:name=".Manage_Applications"
            android:parentActivityName=".Overwrite_Session"
            android:label="Notifications Enabled for "/>

        <activity
            android:name=".Launch"
            android:label="@string/app_name">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

        <activity android:name=".Returning_User"
            android:label="@string/title_returning_home"/>

        <activity android:name=".login" />

        <activity android:name=".New_User"
            android:label="@string/title_home"/>

        <activity android:name=".Start_Session"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="uuid" />
            </intent-filter>
        </activity>

        <activity android:name=".Overwrite_Session"
            android:parentActivityName=".Start_Session"
            android:label="Session and Settings" />

        <activity android:name=".Manage_Location"
            android:label="Manage Location"
            android:parentActivityName=".Overwrite_Session"/>
    </application>

</manifest>